<h1>投稿一覧</h1>
<div  class="contaner" >
    <table class="table">
        <% @posts.each do |post| %>
        <tr>
            <td> <%= link_to post.title, show_path(post) %></td>
        </tr>
        <% end %>
    </table>
</div>
