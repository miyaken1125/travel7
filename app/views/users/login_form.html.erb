<% if @error_message %>
    <div class = "form-error" >
      <%= @error_message %>
    </div>
<% end %>

<%# route でログインページを　login と名前を付与した為、"/loginに変更" %>
 <%= form_tag( "/login" ) do |f| %>
    <p>メールアドレス</p>
    <input name="email" value="<%= @email %>">
    <p>パスワード</p>
    <input type　=　"password" name　=　"password" value　=　"<%= @password %>">
    <input type　=　"submit" value　=　"ログイン">
 <% end %>
